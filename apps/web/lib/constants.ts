/** Must match SESSION_COOKIE_NAME in @governed-sql/core */
export const SESSION_COOKIE_NAME = "gsw_session";

/** Browser requests use the Next.js rewrite prefix. */
export const API_PREFIX = "/api";
